export const meta = {
  // a comment inside meta is fine — the static reader evals the literal
  name: 'good-comments',
  description: 'comments and trailing commas are still a pure literal', /* block */
  phases: [{ title: 'One' }],
}
return 1
