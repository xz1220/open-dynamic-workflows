{
  "name": "@odw/desktop",
  "version": "0.3.0",
  "private": true,
  "description": "Open Dynamic Workflows — read-only desktop client (Tauri shell). Wraps `odw serve`.",
  "type": "module",
  "scripts": {
    "bundle:sidecar": "node scripts/bundle-sidecar.mjs",
    "sidecar:fresh": "npm --prefix ../.. run build:binary && npm run bundle:sidecar",
    "predev": "npm run sidecar:fresh",
    "dev": "tauri dev",
    "dev:fast": "npm run bundle:sidecar && tauri dev",
    "prebuild": "npm run sidecar:fresh",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
